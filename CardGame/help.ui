<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>Help</class>
 <widget class="QDialog" name="Help">
  <property name="geometry">
   <rect>
    <x>0</x>
    <y>0</y>
    <width>400</width>
    <height>248</height>
   </rect>
  </property>
  <property name="minimumSize">
   <size>
    <width>400</width>
    <height>200</height>
   </size>
  </property>
  <property name="maximumSize">
   <size>
    <width>400</width>
    <height>400</height>
   </size>
  </property>
  <property name="windowTitle">
   <string>Dialog</string>
  </property>
  <property name="windowIcon">
   <iconset resource="cards.qrc">
    <normaloff>:/cards/CardIcon.ico</normaloff>:/cards/CardIcon.ico</iconset>
  </property>
  <widget class="QTabWidget" name="tabWidget">
   <property name="geometry">
    <rect>
     <x>0</x>
     <y>0</y>
     <width>400</width>
     <height>251</height>
    </rect>
   </property>
   <property name="autoFillBackground">
    <bool>false</bool>
   </property>
   <property name="currentIndex">
    <number>1</number>
   </property>
   <widget class="QWidget" name="KlondikeTab">
    <attribute name="title">
     <string>Klondike</string>
    </attribute>
    <widget class="QPlainTextEdit" name="plainTextEdit">
     <property name="geometry">
      <rect>
       <x>0</x>
       <y>0</y>
       <width>391</width>
       <height>221</height>
      </rect>
     </property>
     <property name="readOnly">
      <bool>true</bool>
     </property>
     <property name="plainText">
      <string>1.The seven piles of cards in the center of the board are the field piles. 

2.Only a King can be moved onto an empty field pile

3.You may place a card on top of another card if that card is of the 
opposite color and one pip lower than the card to be placed.

4.You may move all the cards from one field pile to another if the stack
to be moved is in the correct sequence and follows the rules above.

5.The pile to the upper left is the Deal pile, click this pile to show the top
three cards on the Dealt pile.

6.Only the top card can be moved off of the dealt pile.

7.The four piles to the top right are the Playoff piles. Cards are placed
in these piles in ascending order according to suit, from Ace to King</string>
     </property>
    </widget>
   </widget>
   <widget class="QWidget" name="FreecellTab">
    <attribute name="title">
     <string>Freecell</string>
    </attribute>
    <widget class="QPlainTextEdit" name="plainTextEdit_2">
     <property name="geometry">
      <rect>
       <x>0</x>
       <y>0</y>
       <width>391</width>
       <height>221</height>
      </rect>
     </property>
     <property name="readOnly">
      <bool>true</bool>
     </property>
     <property name="plainText">
      <string>1.There are 8 field piles in the middle of the game board dealt face up.

2.A card can only be placed on another card if that card is of the opposite
color and if it is 1 pip larger in value than the card being moved.

3.Any card can be placed on an empty field pile

4.The 4 piles to the top right are called the free cells. Only 1 card can be
placed in a free cell at a time.

5.You may move stacks of cards in the field if there is enough free spaces 
available to move all of the cards in the stack above the chosen card.

6.The 4 piles to the top right are the playoff piles. Move cards to these 
piles according to suit in ascending order, ace to king.</string>
     </property>
    </widget>
   </widget>
   <widget class="QWidget" name="GeneralHelpTab">
    <attribute name="title">
     <string>General</string>
    </attribute>
    <widget class="QPlainTextEdit" name="plainTextEdit_3">
     <property name="geometry">
      <rect>
       <x>3</x>
       <y>0</y>
       <width>391</width>
       <height>221</height>
      </rect>
     </property>
     <property name="readOnly">
      <bool>true</bool>
     </property>
     <property name="plainText">
      <string>Solitaire is played with one standard deck of 52 cards.
The games are meant to be played by a single player. 
Anyone found violating the holy rules of play will be shot on 
site and their ashes will be pissed on by a herd of feral cats.</string>
     </property>
    </widget>
   </widget>
  </widget>
 </widget>
 <resources>
  <include location="cards.qrc"/>
 </resources>
 <connections/>
</ui>
